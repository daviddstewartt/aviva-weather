const base = 10;

const Metrics = {
  spacing: {
    xs: base * 0.25,
    s: base * 0.5,
    m: base,
    l: base * 2,
    xl: base * 4,
    xxl: base * 8,
  },
  radius: {
    circle: 500,
    rounded: 5,
    rounded2: 20,
  },
};

export default Metrics;
